# BISONN — Biotic Interactions with Sage Observations using Neural Networks
# Requirements for training (embedding extraction, classification head training,
# evaluation) and inference (Sage plugin).
#
# torch, torchvision, and numpy are NOT listed here — they come from the
# NVIDIA container base image (nvcr.io/nvidia/pytorch:25.08-py3, torch 2.8,
# CUDA 13.0, sm_110/sm_121) and must be FROZEN, not pip-installed.
# See PLAN.md Phase 4 Dockerfile for the freeze constraint pattern.

# --- Sage/Waggle plugin SDK ---
pywaggle[all]==0.56.0

# --- BioCLIP 2.5 Huge model loading + inference ---
open_clip_torch>=2.20.0
timm>=1.0.15
safetensors>=0.4.0
huggingface_hub>=1.0.0

# --- Classification heads (training + inference) ---
scikit-learn>=1.4.0
joblib>=1.3.0

# --- Data acquisition + manifest management ---
datasets>=2.20.0
pandas>=2.2.0
pillow>=10.0.0

# --- Evaluation + visualization ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Convenience wrapper (optional, for taxonomy tasks) ---
pybioclip>=2.0.0
